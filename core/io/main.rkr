struct str {
    data: *char;
    len: usize;
}

impl str {
    @[inline]
    fun from_cstr(data: *char): Self {
        let mut len: usize = 0;
        while data[len] != 0 {
            len = len + 1;
        }
        return Self {
            .data: data,
            .len: len
        };
    }

    @[inline]
    fun as_ptr(&self): *char {
        return self$.data;
    }

    @[inline]
    fun len(&self): usize {
        return self$.len;
    }

    @[inline]
    fun println(&self): void {
        printf(c"%s\n", self.data);
    }

    @[inline]
    fun print(&self): void {
        printf(c"%s", self.data);
    }
}

@extern {
    @[ffi(C, printf)]
    fun printf(fmt: *char, +): void;
}
