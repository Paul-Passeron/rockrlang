
interface Producer {
    type Item;
    fun produce(&self): Item;
}

enum X {}

impl Producer for X {
    type Item = int;

    fun produce(&self): Item {
        return 0;
    }
}

fun main(x: X): int {
    let produced = x.produce();
    return 0;
}
