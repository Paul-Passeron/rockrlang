@include core::vec

fun main(): int {
    let mut v = Vec::new();
    v.push(Vec::new());
    let x = v.unsafe_idx(0);
    let y = x$.push("Hello");
    let z = v.unsafe_idx(0)$.unsafe_idx(0)$;
    return 0;
}
